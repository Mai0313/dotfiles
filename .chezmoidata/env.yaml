# Fallbacks for the keys .chezmoi.toml.tmpl added after is_work, so a machine
# whose config predates them keeps applying without a `chezmoi init`. Any value
# in the config's own [data] wins over these.
is_container: false
is_cloud: false
