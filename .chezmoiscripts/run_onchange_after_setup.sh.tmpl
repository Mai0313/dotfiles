{{- /*
chezmoi-driven bootstrap. Auto-runs during `chezmoi apply` when this rendered
content changes (e.g., editing .chezmoidata/packages.yaml or switching OS /
work env). Each body section is idempotent, so a re-run only installs what
actually changed.

Skipped on Windows (no bash); the .ps1 counterpart handles that.

Body shared with executable_setup.sh.tmpl via .chezmoitemplates/setup-body.sh
so the chezmoi-driven path and the manual ~/setup.sh stay byte-identical.
*/ -}}
{{- if ne .chezmoi.os "windows" -}}
{{ template "setup-body.sh" . }}
{{- end -}}
