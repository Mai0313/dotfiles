# Untracked on 2026-09-08. Keep this entry until every machine has applied
# once, then drop it.
.local/bin/citc

# Untracked on 2026-09-08, the hook it held is not wanted any more. Keep this
# entry until every machine has applied once, then drop it.
.codex/hooks.json

# Moved to ~/.local/bin on 2026-09-23. Keep these entries until every machine
# has applied once, then drop them.
setup.sh
setup.ps1
setup_adb.sh
cleanup.sh

# install-cc installs as ccode since 2026-09-23; the binary it left under the
# old name shadows /usr/bin/cc. Keep this entry until every machine has applied
# once, then drop it.
.local/bin/cc
